# 借鉴点映射方法

> **项目：** GEP Harness — OpenClaw 自进化底座
> **日期：** 2026-08-15
> **理论来源：** EvoMap 张昊阳 GIAC 2026《自进化 Harness 与协作网络》

---

## 1️⃣ 定义

**借鉴点映射**：在把第三方系统的设计决策引入本项目时，每个决策点从多个独立视角找证据，防止单视角导致的认知盲区。

每个借鉴点写 N 条 evidence，每条 evidence = 一个具体概念 + 一个具体类比 + 一个可验证的引用。

---

## 2️⃣ 方法 5 步流程

### Step 1: 锁定借鉴点

来源 = DeepSeek Harness 开源。借鉴点是具体设计决策：

- A. 工具调用前加 Hook 拦截
- B. 事件流 append-only，不可回写
- C. Evolver 半自动（Scan→Signal→Mutate 自动，Solidify 人工）

### Step 2: 每个借鉴点从 N 个视角写 evidence

```
evidence = [
  "用<视角A>的<具体概念>类比<本项目的具体设计>，因为...",   # 视角 A
  "用<视角B>的<具体概念>类比<本项目的具体设计>，因为...",   # 视角 B
  ...
]
```

每条 evidence 必须包含：
- 具体概念（不是抽象词）
- 具体类比（"X 像 Y，因为 Z"）
- 可验证引用（文档路径 / URL）

### Step 3: 验证 N 条 evidence 不能跳过

- 借鉴点**必须**有 evidence
- 关键决策**必须**有 ≥2 个不同视角的证据
- 不能只写 1 条就动手

### Step 4: GEP strict 校验

所有 Gene / Capsule / EvolutionEvent / Mutation 必须通过：
- `compute_asset_id(obj) == obj.asset_id`
- 9 个 required 字段齐全（Gene: type/schema_version/id/category/signals_match/strategy/constraints/validation/asset_id）
- signals_match 不污染（不同借鉴点用不同信号簇）
- blast_radius 在 max_files 之内

### Step 5: Solidify 必走人工审批

Evolver 半自动化的最后一步（Solidify）**绝不豁免人工审批**：
- 自动聚合存在系统性偏差（Arrow 定理：不存在完美聚合方法）
- LLM 有系统性认知盲区（9 大认知错觉：锚定效应 / 确认偏误 / 沉没成本等）
- 人工审批 = 责任的最后兜底

---

## 3️⃣ 借鉴点映射证据

### 借鉴点 A: Append-only 事件流

| 视角 | 具体概念 | 具体类比 | 引用 |
|---|---|---|---|
| 分形自相似 | 数学分形——自相似结构按规则递归 | 事件流按前缀复制即可 fork，无需全局锁 | `/data/disk/RedAgentTeamWiki/BeautifulMathematics/wiki/ch17-分形.md` |
| 信号传导 | 生物学信号传导——信号→响应→记忆 | tool_call_before→after→event_stream 同构 | `/data/disk/RedAgentTeamWiki/cell-biology/wiki/ch15-signal-transduction.md` |
| 再建构记忆 | 长时记忆提取 = 再建构（reconstruction） | append-only 是再建构的底稿，每次读取重建 | `/data/disk/RedAgentTeamWiki/CognitivePsychology/wiki/ch06-longterm_memory_retrieval.md` |
| 自然选择 | 进化——选择压力作用于种群 | 事件流 = Capsule 种群，自然选择作用于其变异体 | `/data/disk/RedAgentTeamWiki/OpenStaxBiology/wiki/ch11-evolution.md` |
| GEP 演化事件 | evomap GEP §2.3 EvolutionEvent 字段 | 本项目的 EvolutionEvent 直接沿用其 schema | `/data/disk/githubwiki/evomap/spec/gep-spec-v1.md` |

### 借鉴点 B: 工具调用流水线

| 视角 | 具体概念 | 具体类比 | 引用 |
|---|---|---|---|
| 算法流水线 | 算法 4 阶段——Hook→Approval→Sandbox→Timeout | 工具调用正好 4 个阶段，边界清晰 | `/data/disk/RedAgentTeamWiki/BeautifulMathematics/wiki/ch12-算法.md` |
| 细胞膜选择性 | 膜结构——选择性通透 = 只让特定分子通过 | PermissionCheck = 只让允许的工具通过；脂筏 = allow_only 白名单 | `/data/disk/RedAgentTeamWiki/cell-biology/wiki/ch11-membrane-structure.md` |
| 中枢执行 | 工作记忆——中枢执行（central executive）调度注意 | Hook 调度器 = 中枢执行，管理工具调用的顺序和优先级 | `/data/disk/RedAgentTeamWiki/CognitivePsychology/wiki/ch05-working_memory.md` |
| 自然选择 | 进化——选择压力作用于工具调用 | 高成功率工具 = 被选择保留，低成功率 = 被淘汰 | `/data/disk/RedAgentTeamWiki/OpenStaxBiology/wiki/ch11-evolution.md` |
| GEP 爆半径 | evomap GEP §1.4 blast radius awareness | 每个 Gene 的 blast_radius 和 constraint.max_files 直接对应 | `/data/disk/githubwiki/evomap/spec/gep-spec-v1.md` |

### 借鉴点 C: Evolver 半自动化

| 视角 | 具体概念 | 具体类比 | 引用 |
|---|---|---|---|
| Arrow 定理 | 数学——不存在完美聚合方法 | 多个 LLM 投票无法产生可靠结果 → 双变体 + 人工兜底是唯一诚实选择 | `/data/disk/RedAgentTeamWiki/BeautifulMathematics/wiki/ch22-arrow-theorem.md` |
| 基因演化 | 生物学——突变→选择→遗传 分子演化机制 | Scan→Mutate→Select 同构于 突变-选择-遗传 | `/data/disk/RedAgentTeamWiki/cell-biology/wiki/ch09-gene-evolution.md` |
| 认知错觉 | 心理学——锚定效应 / 确认偏误 / 沉没成本 | 自动 Solidify 的 LLM 容易落入错觉 → 必须人工二次确认 | `/data/disk/RedAgentTeamWiki/CognitivePsychology/wiki/ch11-reasoning_decision.md` |
| 自然选择 vs 人工选择 | 进化——人工选择比自然选择更精准但代价高 | 全自动 Evolver = 自然选择（快但不可控）；半自动 = 人工选择（慢但可靠）| `/data/disk/RedAgentTeamWiki/OpenStaxBiology/wiki/ch11-evolution.md` |
| distiller 插件 | evomap evolver-claude-code-plugin——4 阶段 | 本 Gene 的 strategy 直接对照 distill→evolve→solidify→review | `/data/disk/githubwiki/evomap/original/evolver-claude-code-plugin/` |

---

## 4️⃣ 关键洞察

### 洞察 1: 不确定性的代价

- 不存在完美聚合方法 → Mutate 必须双变体，Solidify 必须人工
- LLM 有系统性偏差 → 自动 Mutate 不能直接进 Gene 库
- GEP asset_id 必须可验证 → 每个 Gene 必须能 round-trip

### 洞察 2: 边界是稳定性的前提

- 选择性通透 = 只有明确允许的工具才能通过
- 4 阶段流水线 = Hook / Approval / Sandbox / Timeout 每个边界不可跳过
- blast_radius + forbidden_paths = 硬约束，不是软建议

### 洞察 3: 选择压力是过滤机制

- 自然选择作用于种群 → GDI 评分作用于 Gene 候选群体
- EvolutionEvent outcome = success/failed → 自动 Solidify 只接受 success
- 人工审批是最后一道过滤 → 即使 outcome=success 也要人工确认

---

## 5️⃣ 方法复用

下次遇到"借鉴第三方系统 X 给 OpenClaw 用"的场景，直接套用：

1. 读 X 的核心文档（至少 30 分钟）
2. 锁定 N 个借鉴点（每个 = 一个独立 signals 簇）
3. 每个借鉴点从 N 个视角写 evidence（具体概念 + 具体类比 + 可验证引用）
4. 每个借鉴点转 GEP Gene 资产（strict 校验）
5. Solidify 必走人工审批
6. 不引入 Skill，不引入插件运行时

---

## 6️⃣ 反模式（必须避免）

| 反模式 | 后果 | 修正 |
|---|---|---|
| 只写 1 条 evidence | 决策变成"信仰" | 关键决策 ≥2 视角 |
| 把借鉴点合并到 1 Gene | signals 污染 | 每借鉴点 1 Gene |
| Solidify 全自动 | 低质量 Gene 进库 | 必走人工 PR review |
| 跳过 GEP strict 校验 | asset_id 漂移 | 6 资产 100% 校验 |
| 不测就发布 | 线上事故 | 至少 4 pytest + mock 验证 |

---

_方法论确立时间：2026-08-15 · 维护者：devagent_
